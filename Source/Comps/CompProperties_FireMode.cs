using Verse;

namespace VFM_VanillaFireModes.Comps
{
    public class CompProperties_FireMode : CompProperties
    {
        public CompProperties_FireMode()
        {
            compClass = typeof(PawnCompFireMode);
        }
    }
}
