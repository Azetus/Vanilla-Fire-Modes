using Verse;

namespace VFM_VanillaFireModes.Comps
{
    public class VFM_CompProperties_FireMode : CompProperties
    {
        public VFM_CompProperties_FireMode()
        {
            compClass = typeof(VFM_PawnCompFireMode);
        }
    }
}
